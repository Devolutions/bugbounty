# Requirements

- Python 3.12+
- Docker with Linux containers
- OpenSSL (for certificate generation)

# Setup

> **Warning**
> Most variable values in `env.template` are yours to change before running, and it is your
> responsibility to secure them. **Three are not:** `DVLS_ADMIN_USERNAME`, `DVLS_ADMIN_PASSWORD`
> and `DVLS_ENCRYPTION_CONFIG_B64`. The administrator account is baked into the seed database with
> its password hash and nothing rewrites it at restore time, so changing it makes the provisioner
> fail to authenticate against the account it just restored. The encryption config is static by
> design, so findings stay reproducible across every bug bounty instance.
>
> In practice only `DATABASE_USERNAME` / `DATABASE_PASSWORD` (applied when the role is created on a
> fresh install) and `GTW_HOSTNAME` are worth changing. `PG_SUPERUSER_PASSWORD` is regenerated by
> `install.py` on every install, so setting it by hand has no effect.

Copy `env.template` to `.env.local` if you want to override specific values without touching the template (see [.env.local overrides](#envlocal-overrides)).

# Run

**Fresh install** (cleans everything including the `pgdata` volume, generates certs, refreshes
the Docker images, starts the containers, then waits for the server to report healthy and for
the provisioner to finish before restarting the server so it loads the Gateway keypair):

```bash
# Linux (run as root)
sudo python3 install.py [--skip-ca-validation] [--no-cert-gen]

# Windows (run as Administrator)
python install.py [--skip-ca-validation] [--no-cert-gen]
```

**Subsequent starts** (no reinstall, just start containers):

```bash
# Linux
sudo python3 run.py [--update]

# Windows
python run.py [--update]
```

### Optional arguments

**install.py**

| Flag | Description |
|------|-------------|
| `--skip-ca-validation` | Skips installing the CA certificate into the system trust store |
| `--no-cert-gen` | Skips certificate generation — existing certificates in `Certificates/` are used as-is |

**run.py**

| Flag | Description |
|------|-------------|
| `--update` | Rebuilds `.env` from template (preserving the live cluster superuser password), pulls the latest Docker images, force-recreates all containers, waits for the provisioner to exit, then restarts Devolutions Server so it loads the Gateway provisioner keypair |

To access the server go to https://localhost:5544

Log in with `DVLS_ADMIN_USERNAME` / `DVLS_ADMIN_PASSWORD` from your `.env` (`ADMINISTRATOR` /
`SuperS3cure%5`). These are fixed — see the warning under [Setup](#setup).

> **Note**
> `install.py` refreshes the Docker images before starting anything and **aborts** if that pull
> fails, rather than falling back to whatever is cached locally. A stale cached image from before
> the PostgreSQL migration cannot talk to the database at all, so a hard failure here is
> deliberate: fix registry access and re-run.

# Logs

All output from `install.py` and `run.py` is written to `output.log` in the script directory (overwritten on each run). If a script encounters an unexpected error, the console stays open until you press Enter.

# Certificate management

Certificates are stored in the `Certificates/` folder (gitignored). The script automatically:

1. **Generates** a self-signed CA, DVLS server cert, Gateway server cert, and provisioner key pair on first run
2. **Reuses** existing certificates on subsequent runs (no regeneration unless files are missing)
3. **Partial regeneration** — if only some certificates are missing, only the missing ones are regenerated:
   - CA or DVLS missing → full regeneration
   - Only Gateway certs missing → `--gateway-only` (re-signs against existing CA)
   - Only provisioner keys missing → `--provisioner-only`
4. **Injects** all certificates into `.env` as Base64-encoded variables at runtime
5. **Installs** the CA certificate into the system trust store (Linux: `update-ca-certificates` / `update-ca-trust`, Windows: `certutil`)
6. **Configures Firefox** automatically on both Windows and Linux to trust the system CA store (no manual import needed)

To force a full certificate regeneration, delete the `Certificates/` folder and rerun.

# .env.local overrides

Create a `.env.local` file (gitignored) to override specific variables from `env.template` without modifying the template:

```bash
# .env.local — personal overrides, never committed
GTW_HOSTNAME=gateway.custom
DATABASE_PASSWORD=MyCustomPassword123
```

Values in `.env.local` take precedence over `env.template`. Certificate B64 variables are always injected automatically and should not be set manually.

Two overrides that will not do what you expect:

- `DATABASE_PASSWORD` only takes effect on a **fresh install**. The database role is created from it
  when the seed dump is restored, so an existing `pgdata` volume keeps the password it was created
  with. Run `install.py` (which drops the volume) to apply a change.
- `PG_SUPERUSER_PASSWORD` is regenerated by `install.py` on every install and carried across by
  `run.py --update`, so an override here is always discarded.

# .env file documentation

The `.env` file is rebuilt from `env.template` (+ `.env.local` overrides) on every run. **Do not edit `.env` directly** — changes will be overwritten.

### Configurable variables (in `env.template` / `.env.local`)

| Variable | Description |
|----------|-------------|
| `POSTGRES_IMAGE` | PostgreSQL base image the database image was built from (pinned; no inline default) |
| `PG_SUPERUSER` | PostgreSQL cluster superuser name (default: `postgres`) |
| `PG_SUPERUSER_PASSWORD` | Cluster superuser password — **regenerated by `install.py` on every install**, not a DVLS credential |
| `DATABASE_NAME` | Database DVLS connects to (default: `dvls_docker`) |
| `DATABASE_USERNAME` | Role DVLS connects as — created when the seed dump is restored |
| `DATABASE_PASSWORD` | Password for that role — applied on a fresh install only |
| `DVLS_ADMIN_USERNAME` | DVLS login. **Fixed** — baked into the seed database with its password hash |
| `DVLS_ADMIN_PASSWORD` | DVLS password. **Fixed** — see above |
| `DVLS_ACCESS_URIS` | Additional access URIs written by the provisioner (JSON array) |
| `DVLS_ENCRYPTION_CONFIG_B64` | Base64-encoded `encryption.config`. **Static by design** so findings stay reproducible |
| `GTW_HOSTNAME` | Gateway hostname added to the hosts file (default: `gateway.loc`) |

### Auto-injected variables (do not set manually)

These are populated at runtime from files in `Certificates/`:

| Variable | Source file |
|----------|-------------|
| `DVLS_CERT_CRT_B64` | `Certificates/dvls.crt` |
| `DVLS_CERT_KEY_B64` | `Certificates/dvls.key` |
| `DVLS_CA_CERT_B64` | `Certificates/ca.crt` |
| `GTW_TLS_CERTIFICATE_B64` | `Certificates/gtw.crt` |
| `GTW_TLS_PRIVATE_KEY_B64` | `Certificates/gtw.key` |
| `GTW_PROVISIONER_PUBLIC_KEY_B64` | `Certificates/gtw-provisioner.pem` |
| `GTW_PROVISIONER_PRIVATE_KEY_B64` | `Certificates/gtw-provisioner.key` |

# Changelog

- 15/09/2026 - Migrated the stack from SQL Server to PostgreSQL. `sqlserver_db` is replaced by `postgres_db` plus a one-shot `dvls_seed` provisioner; the database now lives in the `pgdata` named volume instead of `data-sql/`. All `SQL_*` variables, `DVLS_CONNECTION_STRING` and `DVLS_CERT_CONFIG` are gone; `DVLS_DECRYPTED_ENCRYPTION_CONFIG_B64` is now `DVLS_ENCRYPTION_CONFIG_B64`. HTTP on 5580 is removed — HTTPS on 5544 only. `install.py` now fails hard if the image pull fails
- 10/06/2026 - Updated containers to v2026.2.5.0
- 28/04/2026 - `run.py --update` now clears `sql.configured` sentinel so SQL Server reconfigures on new images; `.gitignore` updated to properly ignore `Certificates/`, `data-dvls/`, `data-sql/`, `tmp`
- 31/03/2026 - Renamed `env.local` to `.env.local`; `--update` on `run.py` now also rebuilds `.env` and force-recreates containers
- 24/03/2026 - Migrated to Python scripts, added logging to output.log, fixed SQL data folder cleanup
- 17/03/2026 - Improved .env management, certificate handling, and Windows compatibility
- 04/03/2026 - Updated containers to v2026.1.6.0
- 28/01/2026 - Updated containers to v2025.3.14.0
- 23/10/2025 - Updated containers to v2025.3.4.0
- 23/09/2025 - Updated containers to v2025.2.12.0
- 15/07/2025 - Updated containers to v2025.2.5.0
- 15/07/2025 - Updated containers to v2025.2.4.0
